Do not execute commands that modify the environment
(e.g. npm install, pnpm install, yarn install, npm run build,
pnpm build, migrations, code generation, dev servers)
without explicit user approval.

Assume read-only mode by default.